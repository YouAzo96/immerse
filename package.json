{
  "name": "immerse",
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel start:Chatvia start:backend",
    "start:frontend": "cd Chatvia && npm start",
    "start:backend": "cd backend && npm start"
  },
  "dependencies": {
    "shared-library": "^1.0.0"
  }
}
