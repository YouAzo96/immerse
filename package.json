{
  "name": "immerse",
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel start:Immerse start:backend",
    "start:frontend": "cd Immerse && npm start",
    "start:backend": "cd backend && npm start"
  },
  "dependencies": {
    "shared-library": "^1.0.0"
  }
}
